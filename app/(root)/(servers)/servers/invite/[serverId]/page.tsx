const ServerInvitePage = ({ params }: { params: { serverId: string } }) => {
  // Find server by id, if not found redirect to servers page, if found then call server action to invite user to server
};

export default ServerInvitePage;
